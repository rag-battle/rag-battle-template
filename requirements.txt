# =============================================================================
# RAG Battle Template - 공통 의존성
# =============================================================================
# 아래는 최소 공통 의존성이다.
# 팀별로 필요한 패키지는 각자 추가한다. (langchain, chromadb, faiss 등)
# =============================================================================

# 데이터셋 로딩 (Hugging Face datasets)
datasets

# 수치 연산
numpy

# 데이터 처리 및 분석
pandas

# 환경 변수 로딩
python-dotenv
